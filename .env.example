# Database configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=openmrs

# Keycloak configuration
KEYCLOAK_REALM_ROLES=["user"]
KEYCLOAK_DEFAULT_PASSWORD=password

# Output configuration
OUTPUT_FILENAME=keycloak-users.json
SOURCE_SYSTEM=openmrs

# Volume configuration
LOGS_DIR=./logs
OUTPUT_DIR=./output
